<UserControl
  x:Class="Flow.Launcher.Plugin.GitHubRepositoryViewer.UI.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.modernwpf.com/2019"
  xmlns:vm="clr-namespace:Flow.Launcher.Plugin.GitHubRepositoryViewer.UI"
  d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
  d:DesignHeight="350"
  d:DesignWidth="450"
  mc:Ignorable="d"
>
  <Border CornerRadius="0,0,9,9" Style="{DynamicResource SettingGroupBox}">
    <StackPanel Margin="0,10,0,10" Orientation="Horizontal">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" SharedSizeGroup="A" />
          <ColumnDefinition Width="*" SharedSizeGroup="B" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
          <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock
          Grid.Row="0"
          Grid.Column="0"
          Margin="16,0,16,0"
          VerticalAlignment="Center"
          Text="GitHub API Token"
          TextBlock.Foreground="{DynamicResource Color05B}"
        />

        <StackPanel
          Grid.Row="0"
          Grid.Column="1"
          Margin="0,0,6,0"
          Orientation="Horizontal"
          VerticalAlignment="Center"
        >
          <TextBox
            Width="300"
            VerticalAlignment="Center"
            Text="{Binding ApiToken}"
            TextWrapping="NoWrap"
          />
        </StackPanel>
      </Grid>
    </StackPanel>
  </Border>
</UserControl>
