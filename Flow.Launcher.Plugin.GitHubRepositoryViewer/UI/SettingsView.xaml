<UserControl
    x:Class="Flow.Launcher.Plugin.GitHubRepositoryViewer.UI.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.modernwpf.com/2019"
    xmlns:vm="clr-namespace:Flow.Launcher.Plugin.GitHubRepositoryViewer.UI"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="350"
    d:DesignWidth="450"
    mc:Ignorable="d">
    <Grid Margin="{StaticResource SettingPanelMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" SharedSizeGroup="A" />
                <ColumnDefinition Width="*" SharedSizeGroup="B" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
                VerticalAlignment="Center"
                Text="GitHub API Token"
                TextBlock.Foreground="{DynamicResource Color05B}" />

            <TextBox
                Grid.Column="1"
                Margin="{StaticResource SettingPanelItemTopBottomMargin}"
                VerticalAlignment="Center"
                Text="{Binding ApiToken}"
                TextWrapping="NoWrap" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListView
                x:Name="ExcludedOwnersList"
                Grid.Row="0"
                Margin="{StaticResource SettingPanelItemTopBottomMargin}"
                BorderBrush="DarkGray"
                BorderThickness="1"
                ItemsSource="{Binding ExcludedOwnersCollection}"
                SelectionMode="Extended">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="Auto" Header="Excluded Owners" />
                    </GridView>
                </ListView.View>
            </ListView>

            <DockPanel Grid.Row="1">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button
                        MinWidth="100"
                        Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
                        Click="ButtonAdd_Click"
                        Content="Add" />
                    <Button
                        MinWidth="100"
                        Margin="{StaticResource SettingPanelItemTopBottomMargin}"
                        Click="ButtonDelete_Click"
                        Content="Delete" />
                </StackPanel>

                <TextBox x:Name="ExcludedOwnerInput" Margin="{StaticResource SettingPanelItemRightTopBottomMargin}" />
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
